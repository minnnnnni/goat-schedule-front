# 와이어프레임 분석 및 구현 계획

## 📱 전체 페이지 맵

이미지에서 보이는 와이어프레임:

```
와이어프레임
├── 핫핑면 - 카카오...          (로그인 페이지 - 팀원 A)
├── 메인 정보 입력...           (온보딩 페이지 - 팀원 A)
├── 초기 줄                     (불명확)
├── 탤런더                      (캘린더 페이지 - 당신 B)
├── 일비생 관리                 (직원 관리 - 팀원 A)
├── 설정                        (설정 페이지 - 당신 B)
│
├── 디른 이메일로...            (이메일 로그인 - 팀원 A)
│
├── 회원가입                    (회원가입 - 팀원 A)
└── 홈화면
```

---

## 🎯 당신(개발자 B)이 구현할 페이지

### 1️⃣ **탤런더 (Calendar)** 📅
- **경로:** `src/app/(main)/calendar/page.tsx`
- **컴포넌트:** `src/features/schedule/components/MonthlyCalendarView.tsx`
- **UI 요소:**
  - 월간 캘린더 레이아웃
  - 날짜 선택 가능
  - 근무자 목록 표시
  - 이전/다음 월 버튼

**와이어프레임 분석:**
```
┌─────────────────────────┐
│  < 2025년 11월 >         │
├─────────────────────────┤
│ 일 월 화 수 목 금 토    │
├─────────────────────────┤
│ [ 1][ 2][ 3]...         │
│ ...                     │
│ [30]                    │
└─────────────────────────┘
```

---

### 2️⃣ **설정 (Settings)** ⚙️
- **경로:** `src/app/(main)/settings/page.tsx`
- **컴포넌트:**
  - `src/features/settings/components/StoreInfoView.tsx` (매장 정보 조회/수정)
  - `src/features/settings/components/LogoutButton.tsx` (로그아웃)
- **UI 요소:**
  - 매장 정보 표시
  - 수정 버튼
  - 로그아웃 버튼
  - 저장 버튼

**와이어프레임 분석:**
```
┌─────────────────────────┐
│      매장 정보           │
├─────────────────────────┤
│ 매장명: [________]      │
│ 오픈시간: [__] ~ [__]  │
│ 영업요일: □□□□□□□   │
├─────────────────────────┤
│  [저장]      [로그아웃]  │
└─────────────────────────┘
```

---

### 3️⃣ **홈 (Home)** 🏠
- **경로:** `src/app/(main)/page.tsx`
- **컴포넌트:**
  - `src/features/home/components/DailyScheduleView.tsx` (오늘의 근무표)
  - `src/features/home/components/GenerateSchedulePopup.tsx` (자동 생성 팝업)
- **UI 요소:**
  - 오늘 날짜 표시
  - 오늘의 근무 인원 표시
  - "근무표 자동 생성" 버튼
  - 최근 근무표 링크

**와이어프레임 분석:**
```
┌─────────────────────────┐
│   2025년 11월 11일      │
├─────────────────────────┤
│  오늘의 근무자           │
│  [직원 1][직원 2]...    │
├─────────────────────────┤
│  [근무표 자동 생성]     │
└─────────────────────────┘
```

---

### 4️⃣ **근무 추가** (`/calendar/shift/add`) ➕
- **경로:** `src/app/calendar/shift/add/page.tsx`
- **컴포넌트:** `src/features/schedule/components/ShiftForm.tsx` (폼 모드: 생성)
- **UI 요소:**
  - 직원 선택 드롭다운
  - 날짜 선택
  - 시간 선택 (시작/종료)
  - 저장 버튼

**와이어프레임 분석:**
```
┌─────────────────────────┐
│   근무 추가              │
├─────────────────────────┤
│ 직원: [직원 선택 ▼]    │
│ 날짜: [2025-11-11]    │
│ 시간: [09:00] ~ [18:00]│
├─────────────────────────┤
│  [저장]      [취소]      │
└─────────────────────────┘
```

---

### 5️⃣ **근무 수정** (`/calendar/shift/[shiftId]`) ✏️
- **경로:** `src/app/calendar/shift/[shiftId]/page.tsx`
- **컴포넌트:** `src/features/schedule/components/ShiftForm.tsx` (폼 모드: 수정)
- **UI 요소:**
  - 직원 정보 (수정 불가)
  - 날짜 선택
  - 시간 선택
  - 저장 및 삭제 버튼

**와이어프레임 분석:**
```
┌─────────────────────────┐
│   근무 수정              │
├─────────────────────────┤
│ 직원: 김철수 (고정)    │
│ 날짜: [2025-11-11]    │
│ 시간: [09:00] ~ [18:00]│
├─────────────────────────┤
│ [저장] [삭제] [취소]    │
└─────────────────────────┘
```

---

### 6️⃣ **근무표 생성 결과** (`/generate/results`) 🤖
- **경로:** `src/app/generate/results/page.tsx`
- **컴포넌트:** `src/features/schedule/components/ScheduleResultPicker.tsx`
- **UI 요소:**
  - 여러 옵션 선택지
  - 각 옵션별 근무표 미리보기
  - 선택 버튼
  - 확정 버튼

**와이어프레임 분석:**
```
┌─────────────────────────────────────┐
│  근무표 생성 결과                    │
├─────────────────────────────────────┤
│  옵션 1    옵션 2    옵션 3         │
│ ┌──────┐ ┌──────┐ ┌──────┐        │
│ │ 미리 │ │ 미리 │ │ 미리 │        │
│ │ 보기 │ │ 보기 │ │ 보기 │        │
│ └──────┘ └──────┘ └──────┘        │
│  [ ] 선택  [ ] 선택  [●] 선택     │
├─────────────────────────────────────┤
│           [확정]                     │
└─────────────────────────────────────┘
```

---

## 📐 **메인 레이아웃 (Main Layout)**

**경로:** `src/app/(main)/layout.tsx`

모든 페이지에 적용되는 공통 레이아웃:

```
┌──────────────────────────────────┐
│          Header (헤더)             │
│  - 로고 / 사용자 정보             │
├──────────────────────────────────┤
│                                  │
│     {children}                   │
│     (페이지 내용)                │
│                                  │
├──────────────────────────────────┤
│    BottomNavigation (탭)         │
│  [홈] [캘린더] [설정]            │
└──────────────────────────────────┘
```

---

## 🧩 **컴포넌트 구조**

```
src/features/
│
├── home/
│   ├── components/
│   │   ├── DailyScheduleView.tsx      (오늘의 근무표)
│   │   └── GenerateSchedulePopup.tsx  (생성 팝업)
│   └── hooks/
│       └── (필요시 추가)
│
├── schedule/
│   ├── components/
│   │   ├── MonthlyCalendarView.tsx    (월간 캘린더)
│   │   ├── DailyShiftList.tsx         (일별 근무자 목록)
│   │   ├── ShiftForm.tsx              (근무 폼)
│   │   └── ScheduleResultPicker.tsx   (결과 선택)
│   ├── hooks/
│   │   └── useScheduleGenerator.ts    (생성 로직)
│   └── utils/
│       └── scheduleAlgorithm.ts       (알고리즘)
│
└── settings/
    ├── components/
    │   ├── StoreInfoView.tsx          (매장 정보)
    │   └── LogoutButton.tsx           (로그아웃)
    └── hooks/
        └── (필요시 추가)

src/components/
│
├── layout/
│   ├── Header.tsx                     (헤더)
│   └── BottomNavigation.tsx           (탭 네비게이션)
│
└── ui/
    ├── Button.tsx                     (버튼)
    ├── Input.tsx                      (입력 필드)
    ├── Modal.tsx                      (모달)
    ├── Sheet.tsx                      (시트)
    └── Calendar.tsx                   (캘린더 UI)
```

---

## 🎨 **UI 요소별 필요 컴포넌트**

| UI 요소 | 위치 | 사용 페이지 |
|--------|------|-----------|
| **Button** | `components/ui/Button.tsx` | 모든 페이지 |
| **Input** | `components/ui/Input.tsx` | 근무 추가/수정 |
| **Calendar** | `components/ui/Calendar.tsx` | 캘린더 페이지 |
| **Modal** | `components/ui/Modal.tsx` | 생성 팝업 |
| **Select** | (추가 필요) | 직원 선택 |
| **Tabs** | (추가 필요) | 옵션 선택 |

---

## 📊 **데이터 흐름**

```
┌─────────────────┐
│  페이지          │
│  (page.tsx)     │
└────────┬────────┘
         │ import
         ↓
┌─────────────────────────────────┐
│  컴포넌트                        │
│  (DailyScheduleView.tsx)        │
└────────┬────────────────────────┘
         │ useSchedule() Hook 호출
         ↓
┌─────────────────────────────────┐
│  Hook                           │
│  (useSchedule.ts)              │
└────────┬────────────────────────┘
         │ scheduleApi.getToday() 호출
         ↓
┌─────────────────────────────────┐
│  API Service                    │
│  (scheduleApi.ts)              │
└────────┬────────────────────────┘
         │ apiClient 사용
         ↓
┌─────────────────────────────────┐
│  API Client                     │
│  (apiClient.ts)                │
└────────┬────────────────────────┘
         │ 백엔드 API 호출
         ↓
┌─────────────────────────────────┐
│  백엔드 서버                     │
└─────────────────────────────────┘
```

---

## ✅ **구현 우선순위**

### Phase 1: 기본 틀 (1-2일)
1. ✅ 메인 레이아웃 (Header, BottomNavigation)
2. ✅ 페이지 뼈대 (page.tsx 파일들)

### Phase 2: 기능 구현 (3-5일)
1. 홈 페이지 (DailyScheduleView)
2. 캘린더 페이지 (MonthlyCalendarView)
3. 설정 페이지 (StoreInfoView)

### Phase 3: 상세 기능 (5-7일)
1. 근무 추가/수정 (ShiftForm)
2. 생성 결과 (ScheduleResultPicker)
3. 자동 생성 팝업 (GenerateSchedulePopup)

---

## 🚀 **다음 단계**

1. **메인 레이아웃 구현하기**
   - `Header.tsx` 기본 디자인
   - `BottomNavigation.tsx` 탭 네비게이션
   - `(main)/layout.tsx` 레이아웃 구성

2. **첫 번째 페이지: 홈**
   - `src/app/(main)/page.tsx` 생성
   - `DailyScheduleView.tsx` 기본 구조

3. **UI 컴포넌트 준비**
   - Button, Input, Modal 등 개선

---

이제 어디부터 시작할까요? 🎯

1. **메인 레이아웃** 구현부터 할까요?
2. **홈 페이지** 부터 시작할까요?
3. **공통 UI 컴포넌트** 부터 완성할까요?
