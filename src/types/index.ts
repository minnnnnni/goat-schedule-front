export interface Employee { id: string; name: string }
export interface Store { id: string; name: string }
export interface TimeBlock { start: string; end: string }
export interface Shift { id: string; employeeId: string; date: string }

